# Assumptions

- CSV export uses one row per selected item, with metadata columns first and item fields after (Entidad, Gestor, Plazo, Comisión, Instrucción, Materia, Submateria, Línea de trabajo, Año).
